version: '3'

services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
  website:
    build: .
    env_file: .env
    ports:
      - 8080:8080
    volumes:
      - .:/app
    depends_on:
      - db